---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: configs.bgp.purelb.io
spec:
  group: bgp.purelb.io
  names:
    kind: BGPConfiguration
    listKind: BGPConfigurationList
    plural: configs
    shortNames:
    - bgpconfig
    singular: config
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              definedSets:
                items:
                  properties:
                    list:
                      items:
                        type: string
                      type: array
                    name:
                      type: string
                    prefixes:
                      items:
                        description: |-
                          Prefix matches either an IP prefix or a Route Target Constrain prefix -
                          exactly one of them. gobgpd rejects a prefix with both set, so ipPrefix is
                          optional rather than required; without that, rtcPrefix could never be used.
                        properties:
                          ipPrefix:
                            type: string
                          maskLengthMax:
                            format: int32
                            type: integer
                          maskLengthMin:
                            format: int32
                            type: integer
                          rtcPrefix:
                            description: RtcPrefix is a Route Target Constrain prefix,
                              for RTC policy matching.
                            type: string
                        type: object
                        x-kubernetes-validations:
                        - message: exactly one of ipPrefix or rtcPrefix must be set
                          rule: has(self.ipPrefix) != has(self.rtcPrefix)
                      maxItems: 1024
                      type: array
                    type:
                      type: string
                  required:
                  - name
                  - type
                  type: object
                maxItems: 256
                type: array
              dynamicNeighbors:
                items:
                  properties:
                    peerGroup:
                      type: string
                    prefix:
                      type: string
                  required:
                  - peerGroup
                  - prefix
                  type: object
                maxItems: 256
                type: array
              global:
                properties:
                  applyPolicy:
                    properties:
                      exportPolicy:
                        properties:
                          defaultAction:
                            type: string
                          name:
                            type: string
                          policies:
                            items:
                              type: string
                            type: array
                        type: object
                      importPolicy:
                        properties:
                          defaultAction:
                            type: string
                          name:
                            type: string
                          policies:
                            items:
                              type: string
                            type: array
                        type: object
                    type: object
                  asn:
                    description: ASN is the Autonomous System Number for this BGP
                      speaker
                    format: int32
                    type: integer
                  bindToDevice:
                    type: string
                  confederation:
                    properties:
                      enabled:
                        type: boolean
                      identifier:
                        format: int32
                        type: integer
                      memberAsList:
                        items:
                          format: int32
                          type: integer
                        type: array
                    type: object
                  defaultRouteDistance:
                    properties:
                      externalRouteDistance:
                        format: int32
                        type: integer
                      internalRouteDistance:
                        format: int32
                        type: integer
                    type: object
                  families:
                    description: |-
                      Families is the global address-family set. Omit it to keep gobgpd's
                      default of ipv4-unicast and ipv6-unicast.

                      The enum is enforced because the wire encoding is a bare ordinal: an
                      unrecognized name cannot be passed through, and a wrong value does not
                      error - it enables no families at all. See globalFamilyOrdinals.
                    items:
                      enum:
                      - ipv4-unicast
                      - ipv6-unicast
                      - ipv4-labeled
                      - ipv6-labeled
                      - ipv4-vpn
                      - ipv6-vpn
                      - l2vpn-vpls
                      - l2vpn-evpn
                      type: string
                    maxItems: 16
                    type: array
                  gracefulRestart:
                    properties:
                      enabled:
                        type: boolean
                      helperOnly:
                        type: boolean
                      restartTime:
                        format: int32
                        type: integer
                    type: object
                  listenAddresses:
                    items:
                      type: string
                    type: array
                  listenPort:
                    format: int32
                    type: integer
                  nodeStatus:
                    description: NodeStatus configures per-node BGPNodeStatus reporting
                    properties:
                      enabled:
                        description: 'Enabled activates BGPNodeStatus reporting. Default:
                          true.'
                        type: boolean
                      heartbeatSeconds:
                        description: |-
                          HeartbeatSeconds is the interval for unconditional status writes even when state is unchanged.
                          Default: 60. Minimum: 10.
                        format: int32
                        minimum: 10
                        type: integer
                    type: object
                  routeSelectionOptions:
                    properties:
                      advertiseInactiveRoutes:
                        description: AdvertiseInactiveRoutes advertises routes not
                          installed in the FIB.
                        type: boolean
                      alwaysCompareMed:
                        description: AlwaysCompareMed compares MED across paths from
                          different ASes.
                        type: boolean
                      disableBestPathSelection:
                        description: DisableBestPathSelection turns off best-path
                          selection entirely.
                        type: boolean
                      enableAigp:
                        description: EnableAigp enables AIGP metric comparison.
                        type: boolean
                      externalCompareRouterID:
                        description: ExternalCompareRouterID compares router IDs for
                          external paths.
                        type: boolean
                      ignoreAsPathLength:
                        description: IgnoreAsPathLength skips AS-path length in best-path
                          selection.
                        type: boolean
                      ignoreNextHopIgpMetric:
                        description: IgnoreNextHopIgpMetric skips the next-hop IGP
                          metric.
                        type: boolean
                    type: object
                  routerID:
                    description: |-
                      RouterID is the BGP router identifier. Must be a valid IPv4 address.
                      Supports three resolution modes:
                      1. Explicit IPv4: "192.168.1.1" - used as-is
                      2. Template variable: "${NODE_IP}", "${NODE_IPV4}", "${NODE_EXTERNAL_IP}",
                         or "${node.annotations['key']}" - resolved from node
                      3. Auto-detect: when omitted, auto-detects from node's internal IPv4,
                         or generates from node name hash if no IPv4 available
                    type: string
                  routerIDPool:
                    description: |-
                      RouterIDPool is the CIDR pool for hash-based router ID generation.
                      Used when RouterID is omitted and node has no IPv4 address (IPv6-only nodes).
                      Must be at least /24 (256 addresses). Each cluster should use a unique pool.
                      Default: "10.255.0.0/16"
                    type: string
                  useMultiplePaths:
                    type: boolean
                required:
                - asn
                type: object
              neighbors:
                items:
                  description: |-
                    Neighbor is one BGP peer.

                    The BFD/link-local rule below catches a failure with no other symptom: BFD
                    control packets to a link-local address need the scope zone to pick an
                    egress interface, and without one transmit still succeeds on some arbitrary
                    interface. BGP establishes, everything reads healthy, and the BFD session
                    never leaves Down - so the failure detector the operator just enabled is not
                    running. Use `fe80::1%eth0`, or `neighborInterface`, which resolves the zone
                    itself.
                  properties:
                    afiSafis:
                      items:
                        properties:
                          addPaths:
                            properties:
                              receive:
                                type: boolean
                              sendMax:
                                format: int32
                                type: integer
                            type: object
                          enabled:
                            type: boolean
                          family:
                            description: |-
                              Family is the address family, e.g. ipv4-unicast.

                              Enforced as an enum because the converter previously recognized only
                              three names and sent AFI_UNSPECIFIED for anything else - so a typo, or a
                              perfectly valid family it did not know, was accepted by the CRD and
                              silently produced a peer with no usable family.
                            enum:
                            - ipv4-unicast
                            - ipv6-unicast
                            - ipv4-labeled
                            - ipv6-labeled
                            - ipv4-vpn
                            - ipv6-vpn
                            - l2vpn-vpls
                            - l2vpn-evpn
                            - ipv4-flowspec
                            - ipv6-flowspec
                            - rtc
                            type: string
                          prefixLimit:
                            properties:
                              maxPrefixes:
                                format: int32
                                type: integer
                              shutdownThresholdPct:
                                format: int32
                                type: integer
                            required:
                            - maxPrefixes
                            type: object
                        required:
                        - enabled
                        - family
                        type: object
                      type: array
                    applyPolicy:
                      properties:
                        exportPolicy:
                          properties:
                            defaultAction:
                              type: string
                            name:
                              type: string
                            policies:
                              items:
                                type: string
                              type: array
                          type: object
                        importPolicy:
                          properties:
                            defaultAction:
                              type: string
                            name:
                              type: string
                            policies:
                              items:
                                type: string
                              type: array
                          type: object
                      type: object
                    bfd:
                      description: |-
                        BFD, if set, overrides whatever the peer group specifies. Omit it to
                        inherit. See the BFD type for why the override is whole-block.
                      properties:
                        desiredMinimumTxInterval:
                          default: 1000000
                          description: |-
                            DesiredMinimumTxInterval is the minimum interval, in MICROSECONDS,
                            between transmitted control packets. 300000 is 300ms; 300 is 300
                            microseconds.
                          format: int32
                          maximum: 30000000
                          minimum: 300000
                          type: integer
                        detectionMultiplier:
                          default: 3
                          description: |-
                            DetectionMultiplier is how many intervals may be missed before the
                            session is declared down. Detection time is
                            requiredMinimumReceive x detectionMultiplier.
                          format: int32
                          maximum: 255
                          minimum: 3
                          type: integer
                        enabled:
                          description: |-
                            Enabled activates BFD for this session.

                            A pointer so that `enabled: false` survives serialization: as a plain
                            bool with omitempty it would be indistinguishable from an absent block,
                            and an absent block means "inherit from the peer group".
                          type: boolean
                        port:
                          default: 3784
                          description: |-
                            Port is the destination UDP port for control packets sent to this peer.

                            The local listener is always 3784 regardless of this value, so setting
                            anything else means transmitting to a port the peer is probably not
                            listening on. RFC 5881 mandates 3784; there is rarely a reason to change
                            it.
                          format: int32
                          maximum: 65535
                          minimum: 1024
                          type: integer
                        requiredMinimumReceive:
                          default: 1000000
                          description: |-
                            RequiredMinimumReceive is the minimum interval, in MICROSECONDS, between
                            received control packets.
                          format: int32
                          maximum: 30000000
                          minimum: 300000
                          type: integer
                      type: object
                    config:
                      properties:
                        adminDown:
                          type: boolean
                        allowAspathLoopLocal:
                          description: AllowAspathLoopLocal permits a local AS-path
                            loop.
                          type: boolean
                        allowOwnAsn:
                          description: |-
                            AllowOwnAsn permits our own ASN to appear this many times in a received
                            AS path. gobgpd errors above MaxUint8.
                          format: int32
                          maximum: 255
                          type: integer
                        authPassword:
                          description: 'AuthPassword is the BGP authentication password
                            (DEPRECATED: use AuthPasswordSecretRef instead)'
                          type: string
                        authPasswordSecretRef:
                          description: |-
                            AuthPasswordSecretRef references a Secret containing the BGP authentication password
                            The Secret must contain a key matching the neighbor address or a default key "password"
                          properties:
                            key:
                              description: The key of the secret to select from.  Must
                                be a valid secret key.
                              type: string
                            name:
                              default: ""
                              description: |-
                                Name of the referent.
                                This field is effectively required, but due to backwards compatibility is
                                allowed to be empty. Instances of this type with an empty value here are
                                almost certainly wrong.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              type: string
                            optional:
                              description: Specify whether the Secret or its key must
                                be defined
                              type: boolean
                          required:
                          - key
                          type: object
                          x-kubernetes-map-type: atomic
                        description:
                          type: string
                        localAsn:
                          format: int32
                          type: integer
                        neighborAddress:
                          description: |-
                            NeighborAddress is the peer's IP address. For a link-local IPv6 peer,
                            include the scope zone: fe80::1%eth0.

                            The length bound is not cosmetic. CEL rules are costed statically against
                            the declared maxLength, so an unbounded string here makes the BFD
                            link-local rule on Neighbor exceed the apiserver's per-rule cost budget
                            by ~64x and the CRD is rejected outright. 64 = 45 (the longest textual
                            IPv6 form, ::ffff:255.255.255.255) + '%' + 15 (IFNAMSIZ-1), rounded up.
                          maxLength: 64
                          type: string
                        neighborInterface:
                          type: string
                        peerAsn:
                          format: int32
                          type: integer
                        peerGroup:
                          type: string
                        removePrivate:
                          description: |-
                            RemovePrivate strips private ASNs from advertised AS paths:
                            "all" removes them, "replace" substitutes our own ASN.
                          enum:
                          - all
                          - replace
                          type: string
                        replacePeerAsn:
                          description: ReplacePeerAsn rewrites the peer's ASN with
                            ours in advertised AS paths.
                          type: boolean
                        routeFlapDamping:
                          description: RouteFlapDamping enables RFC 2439 damping for
                            this peer.
                          type: boolean
                        sendSoftwareVersion:
                          description: SendSoftwareVersion advertises the software-version
                            capability (RFC 9384).
                          type: boolean
                        vrf:
                          type: string
                      required:
                      - peerAsn
                      type: object
                    ebgpMultihop:
                      properties:
                        enabled:
                          type: boolean
                        multihopTtl:
                          format: int32
                          type: integer
                      required:
                      - enabled
                      type: object
                    gracefulRestart:
                      properties:
                        enabled:
                          type: boolean
                        helperOnly:
                          type: boolean
                        restartTime:
                          format: int32
                          type: integer
                      type: object
                    nodeSelector:
                      description: |-
                        A label selector is a label query over a set of resources. The result of matchLabels and
                        matchExpressions are ANDed. An empty label selector matches all objects. A null
                        label selector matches no objects.
                      properties:
                        matchExpressions:
                          description: matchExpressions is a list of label selector
                            requirements. The requirements are ANDed.
                          items:
                            description: |-
                              A label selector requirement is a selector that contains values, a key, and an operator that
                              relates the key and values.
                            properties:
                              key:
                                description: key is the label key that the selector
                                  applies to.
                                type: string
                              operator:
                                description: |-
                                  operator represents a key's relationship to a set of values.
                                  Valid operators are In, NotIn, Exists and DoesNotExist.
                                type: string
                              values:
                                description: |-
                                  values is an array of string values. If the operator is In or NotIn,
                                  the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                  the values array must be empty. This array is replaced during a strategic
                                  merge patch.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: atomic
                            required:
                            - key
                            - operator
                            type: object
                          type: array
                          x-kubernetes-list-type: atomic
                        matchLabels:
                          additionalProperties:
                            type: string
                          description: |-
                            matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                            map is equivalent to an element of matchExpressions, whose key field is "key", the
                            operator is "In", and the values array contains only "value". The requirements are ANDed.
                          type: object
                      type: object
                      x-kubernetes-map-type: atomic
                    routeReflector:
                      properties:
                        routeReflectorClient:
                          type: boolean
                        routeReflectorClusterId:
                          type: string
                      required:
                      - routeReflectorClient
                      - routeReflectorClusterId
                      type: object
                    timers:
                      properties:
                        config:
                          properties:
                            connectRetry:
                              format: int64
                              type: integer
                            holdTime:
                              format: int64
                              type: integer
                            keepaliveInterval:
                              format: int64
                              type: integer
                            minimumAdvertisementInterval:
                              format: int64
                              type: integer
                          type: object
                      required:
                      - config
                      type: object
                    transport:
                      properties:
                        bindInterface:
                          type: string
                        ipTos:
                          description: |-
                            IpTos sets IP_TOS / IPV6_TCLASS on the BGP session.

                            Bounded at 255 because it is a single IP header byte and gobgpd casts it
                            to uint8 with no range check - an unbounded value would be silently
                            truncated rather than rejected.
                          format: int32
                          maximum: 255
                          type: integer
                        localAddress:
                          type: string
                        passiveMode:
                          type: boolean
                      type: object
                  required:
                  - config
                  type: object
                  x-kubernetes-validations:
                  - message: a link-local neighborAddress needs its scope zone when
                      BFD is enabled (fe80::1%eth0, not fe80::1); or use neighborInterface
                      instead
                    rule: '!has(self.bfd) || (has(self.bfd.enabled) && !self.bfd.enabled)
                      || !has(self.config.neighborAddress) || !self.config.neighborAddress.lowerAscii().startsWith(''fe80'')
                      || self.config.neighborAddress.contains(''%'')'
                maxItems: 1024
                type: array
              netlinkExport:
                description: NetlinkExport configures global netlink export for exporting
                  BGP routes to Linux kernel
                properties:
                  dampeningInterval:
                    description: 'DampeningInterval in milliseconds to prevent flapping
                      (default: 100)'
                    format: int32
                    type: integer
                  enabled:
                    description: Enabled activates netlink export functionality
                    type: boolean
                  routeProtocol:
                    description: 'RouteProtocol is the Linux route protocol identifier
                      (default: 186 = RTPROT_BGP)'
                    format: int32
                    type: integer
                  rules:
                    description: Rules defines export rules for matching and exporting
                      routes
                    items:
                      description: NetlinkExportRule defines a rule for exporting
                        routes to Linux kernel
                      properties:
                        communityList:
                          description: 'CommunityList filters routes by standard BGP
                            communities (format: "AS:VALUE" where AS and VALUE are
                            0-65535)'
                          items:
                            pattern: ^\d{1,5}:\d{1,5}$
                            type: string
                          maxItems: 128
                          type: array
                        largeCommunityList:
                          description: 'LargeCommunityList filters routes by large
                            BGP communities (format: "ASN:LocalData1:LocalData2")'
                          items:
                            pattern: ^\d+:\d+:\d+$
                            type: string
                          maxItems: 128
                          type: array
                        metric:
                          description: 'Metric is the route metric/priority in Linux
                            routing table (default: 20)'
                          format: int32
                          type: integer
                        name:
                          description: Name is a unique identifier for this export
                            rule
                          type: string
                        tableId:
                          description: TableId is the Linux routing table ID (0 =
                            main table)
                          format: int32
                          type: integer
                        validateNexthop:
                          description: 'ValidateNexthop enables nexthop reachability
                            validation before exporting (default: true)'
                          type: boolean
                        vrf:
                          description: Vrf is the target VRF name (empty = global
                            routing table)
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                type: object
              netlinkImport:
                description: NetlinkImport configures global netlink import for importing
                  connected routes
                properties:
                  enabled:
                    description: Enabled activates netlink import functionality
                    type: boolean
                  interfaceList:
                    description: InterfaceList specifies interfaces to import routes
                      from (supports glob patterns like "eth*")
                    items:
                      type: string
                    type: array
                  vrf:
                    description: Vrf specifies the VRF to import routes into (empty
                      = global RIB)
                    type: string
                type: object
              peerGroups:
                items:
                  properties:
                    afiSafis:
                      items:
                        properties:
                          addPaths:
                            properties:
                              receive:
                                type: boolean
                              sendMax:
                                format: int32
                                type: integer
                            type: object
                          enabled:
                            type: boolean
                          family:
                            description: |-
                              Family is the address family, e.g. ipv4-unicast.

                              Enforced as an enum because the converter previously recognized only
                              three names and sent AFI_UNSPECIFIED for anything else - so a typo, or a
                              perfectly valid family it did not know, was accepted by the CRD and
                              silently produced a peer with no usable family.
                            enum:
                            - ipv4-unicast
                            - ipv6-unicast
                            - ipv4-labeled
                            - ipv6-labeled
                            - ipv4-vpn
                            - ipv6-vpn
                            - l2vpn-vpls
                            - l2vpn-evpn
                            - ipv4-flowspec
                            - ipv6-flowspec
                            - rtc
                            type: string
                          prefixLimit:
                            properties:
                              maxPrefixes:
                                format: int32
                                type: integer
                              shutdownThresholdPct:
                                format: int32
                                type: integer
                            required:
                            - maxPrefixes
                            type: object
                        required:
                        - enabled
                        - family
                        type: object
                      type: array
                    applyPolicy:
                      properties:
                        exportPolicy:
                          properties:
                            defaultAction:
                              type: string
                            name:
                              type: string
                            policies:
                              items:
                                type: string
                              type: array
                          type: object
                        importPolicy:
                          properties:
                            defaultAction:
                              type: string
                            name:
                              type: string
                            policies:
                              items:
                                type: string
                              type: array
                          type: object
                      type: object
                    bfd:
                      description: BFD applies to every member that does not specify
                        its own.
                      properties:
                        desiredMinimumTxInterval:
                          default: 1000000
                          description: |-
                            DesiredMinimumTxInterval is the minimum interval, in MICROSECONDS,
                            between transmitted control packets. 300000 is 300ms; 300 is 300
                            microseconds.
                          format: int32
                          maximum: 30000000
                          minimum: 300000
                          type: integer
                        detectionMultiplier:
                          default: 3
                          description: |-
                            DetectionMultiplier is how many intervals may be missed before the
                            session is declared down. Detection time is
                            requiredMinimumReceive x detectionMultiplier.
                          format: int32
                          maximum: 255
                          minimum: 3
                          type: integer
                        enabled:
                          description: |-
                            Enabled activates BFD for this session.

                            A pointer so that `enabled: false` survives serialization: as a plain
                            bool with omitempty it would be indistinguishable from an absent block,
                            and an absent block means "inherit from the peer group".
                          type: boolean
                        port:
                          default: 3784
                          description: |-
                            Port is the destination UDP port for control packets sent to this peer.

                            The local listener is always 3784 regardless of this value, so setting
                            anything else means transmitting to a port the peer is probably not
                            listening on. RFC 5881 mandates 3784; there is rarely a reason to change
                            it.
                          format: int32
                          maximum: 65535
                          minimum: 1024
                          type: integer
                        requiredMinimumReceive:
                          default: 1000000
                          description: |-
                            RequiredMinimumReceive is the minimum interval, in MICROSECONDS, between
                            received control packets.
                          format: int32
                          maximum: 30000000
                          minimum: 300000
                          type: integer
                      type: object
                    config:
                      properties:
                        allowAspathLoopLocal:
                          description: AllowAspathLoopLocal permits our own ASN in
                            locally originated paths.
                          type: boolean
                        allowOwnAsn:
                          description: |-
                            AllowOwnAsn permits this many occurrences of our own ASN in a received
                            AS path. Bounded at 255 because gobgpd rejects anything larger.
                          format: int32
                          maximum: 255
                          type: integer
                        authPassword:
                          description: 'AuthPassword is the BGP authentication password
                            (DEPRECATED: use AuthPasswordSecretRef instead)'
                          type: string
                        authPasswordSecretRef:
                          description: AuthPasswordSecretRef references a Secret containing
                            the BGP authentication password
                          properties:
                            key:
                              description: The key of the secret to select from.  Must
                                be a valid secret key.
                              type: string
                            name:
                              default: ""
                              description: |-
                                Name of the referent.
                                This field is effectively required, but due to backwards compatibility is
                                allowed to be empty. Instances of this type with an empty value here are
                                almost certainly wrong.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              type: string
                            optional:
                              description: Specify whether the Secret or its key must
                                be defined
                              type: boolean
                          required:
                          - key
                          type: object
                          x-kubernetes-map-type: atomic
                        description:
                          type: string
                        localAsn:
                          format: int32
                          type: integer
                        peerAsn:
                          format: int32
                          type: integer
                        peerGroupName:
                          type: string
                        removePrivate:
                          description: |-
                            RemovePrivate strips private ASNs from advertised AS paths:
                            "all" removes them, "replace" substitutes our own ASN.
                          enum:
                          - all
                          - replace
                          type: string
                        replacePeerAsn:
                          description: ReplacePeerAsn replaces the peer's ASN in the
                            AS path with our own.
                          type: boolean
                        routeFlapDamping:
                          description: RouteFlapDamping enables RFC 2439 damping for
                            members of this group.
                          type: boolean
                        sendSoftwareVersion:
                          description: SendSoftwareVersion advertises the software-version
                            capability (RFC 9384).
                          type: boolean
                      required:
                      - peerGroupName
                      type: object
                    nodeSelector:
                      description: |-
                        A label selector is a label query over a set of resources. The result of matchLabels and
                        matchExpressions are ANDed. An empty label selector matches all objects. A null
                        label selector matches no objects.
                      properties:
                        matchExpressions:
                          description: matchExpressions is a list of label selector
                            requirements. The requirements are ANDed.
                          items:
                            description: |-
                              A label selector requirement is a selector that contains values, a key, and an operator that
                              relates the key and values.
                            properties:
                              key:
                                description: key is the label key that the selector
                                  applies to.
                                type: string
                              operator:
                                description: |-
                                  operator represents a key's relationship to a set of values.
                                  Valid operators are In, NotIn, Exists and DoesNotExist.
                                type: string
                              values:
                                description: |-
                                  values is an array of string values. If the operator is In or NotIn,
                                  the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                  the values array must be empty. This array is replaced during a strategic
                                  merge patch.
                                items:
                                  type: string
                                type: array
                                x-kubernetes-list-type: atomic
                            required:
                            - key
                            - operator
                            type: object
                          type: array
                          x-kubernetes-list-type: atomic
                        matchLabels:
                          additionalProperties:
                            type: string
                          description: |-
                            matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                            map is equivalent to an element of matchExpressions, whose key field is "key", the
                            operator is "In", and the values array contains only "value". The requirements are ANDed.
                          type: object
                      type: object
                      x-kubernetes-map-type: atomic
                    timers:
                      properties:
                        config:
                          properties:
                            connectRetry:
                              format: int64
                              type: integer
                            holdTime:
                              format: int64
                              type: integer
                            keepaliveInterval:
                              format: int64
                              type: integer
                            minimumAdvertisementInterval:
                              format: int64
                              type: integer
                          type: object
                      required:
                      - config
                      type: object
                    transport:
                      properties:
                        bindInterface:
                          type: string
                        ipTos:
                          description: |-
                            IpTos sets IP_TOS / IPV6_TCLASS on the BGP session.

                            Bounded at 255 because it is a single IP header byte and gobgpd casts it
                            to uint8 with no range check - an unbounded value would be silently
                            truncated rather than rejected.
                          format: int32
                          maximum: 255
                          type: integer
                        localAddress:
                          type: string
                        passiveMode:
                          type: boolean
                      type: object
                  required:
                  - config
                  type: object
                maxItems: 256
                type: array
              policyDefinitions:
                items:
                  properties:
                    name:
                      type: string
                    statements:
                      description: |-
                        Statements are evaluated in order.

                        Bounded because CEL rules nested below here are costed statically
                        against this bound; unbounded, the estimator assumes the maximum and
                        pushed the whole CRD over the apiserver budget, rejecting it outright.
                      items:
                        properties:
                          actions:
                            properties:
                              asPrepend:
                                properties:
                                  asn:
                                    format: int32
                                    type: integer
                                  repeat:
                                    format: int32
                                    type: integer
                                required:
                                - asn
                                type: object
                              community:
                                properties:
                                  communities:
                                    description: Communities are the values to act
                                      on.
                                    items:
                                      type: string
                                    maxItems: 64
                                    type: array
                                  type:
                                    description: Type is "add", "remove" or "replace".
                                    enum:
                                    - add
                                    - remove
                                    - replace
                                    type: string
                                required:
                                - communities
                                - type
                                type: object
                              extCommunity:
                                description: ExtCommunity manipulates extended communities.
                                properties:
                                  communities:
                                    description: Communities are the values to act
                                      on.
                                    items:
                                      type: string
                                    maxItems: 64
                                    type: array
                                  type:
                                    description: Type is "add", "remove" or "replace".
                                    enum:
                                    - add
                                    - remove
                                    - replace
                                    type: string
                                required:
                                - communities
                                - type
                                type: object
                              largeCommunity:
                                description: LargeCommunity manipulates large communities.
                                properties:
                                  communities:
                                    description: Communities are the values to act
                                      on.
                                    items:
                                      type: string
                                    maxItems: 64
                                    type: array
                                  type:
                                    description: Type is "add", "remove" or "replace".
                                    enum:
                                    - add
                                    - remove
                                    - replace
                                    type: string
                                required:
                                - communities
                                - type
                                type: object
                              localPref:
                                format: int32
                                type: integer
                              med:
                                properties:
                                  type:
                                    description: Type is "mod" (adjust relative to
                                      the current MED) or "replace".
                                    enum:
                                    - mod
                                    - replace
                                    type: string
                                  value:
                                    description: Value is the amount to add, or the
                                      value to set.
                                    format: int64
                                    type: integer
                                required:
                                - type
                                - value
                                type: object
                              nexthop:
                                description: Nexthop rewrites the route's next hop.
                                properties:
                                  address:
                                    description: Address sets an explicit next hop.
                                    maxLength: 64
                                    type: string
                                  peerAddress:
                                    description: PeerAddress rewrites the next hop
                                      to the peer's address.
                                    type: boolean
                                  self:
                                    description: Self rewrites the next hop to this
                                      speaker's address.
                                    type: boolean
                                  unchanged:
                                    description: Unchanged preserves the received
                                      next hop.
                                    type: boolean
                                type: object
                                x-kubernetes-validations:
                                - message: set at most one of address, self, unchanged,
                                    peerAddress
                                  rule: '[has(self.address), has(self.self) && self.self,
                                    has(self.unchanged) && self.unchanged, has(self.peerAddress)
                                    && self.peerAddress].filter(x, x).size() <= 1'
                              origin:
                                description: Origin sets the ORIGIN attribute.
                                enum:
                                - igp
                                - egp
                                - incomplete
                                type: string
                              routeAction:
                                type: string
                            required:
                            - routeAction
                            type: object
                          conditions:
                            properties:
                              afiSafiIn:
                                description: AfiSafiIn matches when the route belongs
                                  to one of these families.
                                items:
                                  enum:
                                  - ipv4-unicast
                                  - ipv6-unicast
                                  - ipv4-labeled
                                  - ipv6-labeled
                                  - ipv4-vpn
                                  - ipv6-vpn
                                  - l2vpn-vpls
                                  - l2vpn-evpn
                                  - ipv4-flowspec
                                  - ipv6-flowspec
                                  - rtc
                                  type: string
                                maxItems: 16
                                type: array
                              asPathLength:
                                description: AsPathLength matches on the number of
                                  ASes in the AS path.
                                properties:
                                  length:
                                    description: Length is the number of ASes to compare
                                      against.
                                    format: int32
                                    type: integer
                                  operator:
                                    description: Operator is "eq", "ge" or "le".
                                    enum:
                                    - eq
                                    - ge
                                    - le
                                    type: string
                                required:
                                - length
                                - operator
                                type: object
                              asPathSet:
                                description: |-
                                  MatchSet references a DefinedSet and says how its members must match.

                                  match was previously accepted and dropped: crdToAPIMatchSet set only Name, so
                                  "all" and "invert" were both sent as TYPE_UNSPECIFIED and the policy matched
                                  differently than it read. That is a silent change of routing policy, not an
                                  inert no-op, which is why the values are now an enforced enum.
                                properties:
                                  match:
                                    description: Match is "any" (default), "all",
                                      or "invert".
                                    enum:
                                    - any
                                    - all
                                    - invert
                                    type: string
                                  name:
                                    description: Name of the DefinedSet to match against.
                                    type: string
                                required:
                                - name
                                type: object
                              communityCount:
                                description: CommunityCount matches on how many communities
                                  a route carries.
                                properties:
                                  count:
                                    description: Count is the number of communities
                                      to compare against.
                                    format: int32
                                    type: integer
                                  operator:
                                    description: Operator is "eq", "ge" or "le".
                                    enum:
                                    - eq
                                    - ge
                                    - le
                                    type: string
                                required:
                                - count
                                - operator
                                type: object
                              communitySet:
                                description: |-
                                  MatchSet references a DefinedSet and says how its members must match.

                                  match was previously accepted and dropped: crdToAPIMatchSet set only Name, so
                                  "all" and "invert" were both sent as TYPE_UNSPECIFIED and the policy matched
                                  differently than it read. That is a silent change of routing policy, not an
                                  inert no-op, which is why the values are now an enforced enum.
                                properties:
                                  match:
                                    description: Match is "any" (default), "all",
                                      or "invert".
                                    enum:
                                    - any
                                    - all
                                    - invert
                                    type: string
                                  name:
                                    description: Name of the DefinedSet to match against.
                                    type: string
                                required:
                                - name
                                type: object
                              extCommunitySet:
                                description: ExtCommunitySet matches extended communities.
                                properties:
                                  match:
                                    description: Match is "any" (default), "all",
                                      or "invert".
                                    enum:
                                    - any
                                    - all
                                    - invert
                                    type: string
                                  name:
                                    description: Name of the DefinedSet to match against.
                                    type: string
                                required:
                                - name
                                type: object
                              largeCommunitySet:
                                description: |-
                                  LargeCommunitySet matches large communities. Note NetlinkExportRule
                                  already supported large communities; BGP policy did not.
                                properties:
                                  match:
                                    description: Match is "any" (default), "all",
                                      or "invert".
                                    enum:
                                    - any
                                    - all
                                    - invert
                                    type: string
                                  name:
                                    description: Name of the DefinedSet to match against.
                                    type: string
                                required:
                                - name
                                type: object
                              localPrefEq:
                                description: |-
                                  LocalPrefEq matches an exact LOCAL_PREF. A pointer because 0 is a valid
                                  local preference and must be distinguishable from "not matching on it".
                                format: int32
                                type: integer
                              medEq:
                                description: MedEq matches an exact MED. A pointer
                                  for the same reason as LocalPrefEq.
                                format: int32
                                type: integer
                              neighborSet:
                                description: |-
                                  MatchSet references a DefinedSet and says how its members must match.

                                  match was previously accepted and dropped: crdToAPIMatchSet set only Name, so
                                  "all" and "invert" were both sent as TYPE_UNSPECIFIED and the policy matched
                                  differently than it read. That is a silent change of routing policy, not an
                                  inert no-op, which is why the values are now an enforced enum.
                                properties:
                                  match:
                                    description: Match is "any" (default), "all",
                                      or "invert".
                                    enum:
                                    - any
                                    - all
                                    - invert
                                    type: string
                                  name:
                                    description: Name of the DefinedSet to match against.
                                    type: string
                                required:
                                - name
                                type: object
                              nextHopInList:
                                description: NextHopInList matches when the next hop
                                  is one of these addresses.
                                items:
                                  type: string
                                maxItems: 64
                                type: array
                              origin:
                                description: Origin matches the ORIGIN attribute.
                                enum:
                                - igp
                                - egp
                                - incomplete
                                type: string
                              prefixSet:
                                description: |-
                                  MatchSet references a DefinedSet and says how its members must match.

                                  match was previously accepted and dropped: crdToAPIMatchSet set only Name, so
                                  "all" and "invert" were both sent as TYPE_UNSPECIFIED and the policy matched
                                  differently than it read. That is a silent change of routing policy, not an
                                  inert no-op, which is why the values are now an enforced enum.
                                properties:
                                  match:
                                    description: Match is "any" (default), "all",
                                      or "invert".
                                    enum:
                                    - any
                                    - all
                                    - invert
                                    type: string
                                  name:
                                    description: Name of the DefinedSet to match against.
                                    type: string
                                required:
                                - name
                                type: object
                              routeType:
                                description: RouteType matches the route's origin
                                  relative to this speaker.
                                enum:
                                - internal
                                - external
                                - local
                                type: string
                              rpkiResult:
                                type: string
                            type: object
                          name:
                            type: string
                        required:
                        - actions
                        - conditions
                        - name
                        type: object
                      maxItems: 128
                      type: array
                  required:
                  - name
                  - statements
                  type: object
                maxItems: 256
                type: array
              vrfs:
                items:
                  properties:
                    exportRt:
                      items:
                        type: string
                      type: array
                    importRt:
                      items:
                        type: string
                      type: array
                    name:
                      type: string
                    netlinkExport:
                      description: VrfNetlinkExport configures netlink export for
                        a specific VRF
                      properties:
                        communityList:
                          description: 'CommunityList filters routes by standard BGP
                            communities (format: "AS:VALUE" where AS and VALUE are
                            0-65535)'
                          items:
                            pattern: ^\d{1,5}:\d{1,5}$
                            type: string
                          maxItems: 128
                          type: array
                        enabled:
                          description: Enabled activates VRF-to-VRF export
                          type: boolean
                        largeCommunityList:
                          description: 'LargeCommunityList filters routes by large
                            BGP communities (format: "ASN:LocalData1:LocalData2")'
                          items:
                            pattern: ^\d+:\d+:\d+$
                            type: string
                          maxItems: 128
                          type: array
                        linuxTableId:
                          description: LinuxTableId is the target Linux routing table
                            ID (auto-lookup from Linux VRF if not specified)
                          format: int32
                          type: integer
                        linuxVrf:
                          description: LinuxVrf is the target Linux VRF name (defaults
                            to GoBGP VRF name)
                          type: string
                        metric:
                          description: Metric is the route metric/priority in Linux
                            routing table
                          format: int32
                          type: integer
                        validateNexthop:
                          description: 'ValidateNexthop enables nexthop reachability
                            validation before exporting (default: true)'
                          type: boolean
                      type: object
                    netlinkImport:
                      description: VrfNetlinkImport configures netlink import for
                        a specific VRF
                      properties:
                        enabled:
                          description: Enabled activates netlink import for this VRF
                          type: boolean
                        interfaceList:
                          description: InterfaceList specifies interfaces to import
                            routes from (supports glob patterns)
                          items:
                            type: string
                          type: array
                      type: object
                    rd:
                      type: string
                  required:
                  - name
                  type: object
                maxItems: 256
                type: array
            required:
            - global
            type: object
          status:
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the BGP configuration state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              lastReconcileTime:
                description: LastReconcileTime is the timestamp of the last reconciliation
                format: date-time
                type: string
              message:
                description: Message provides additional information about the current
                  state
                type: string
              neighborCount:
                description: NeighborCount is the number of configured BGP neighbors
                type: integer
              observedGeneration:
                description: ObservedGeneration is the most recent generation observed
                  by the controller
                format: int64
                type: integer
              routerIDResolutionTime:
                description: RouterIDResolutionTime is when the router ID was resolved
                format: date-time
                type: string
              routerIDSource:
                description: |-
                  RouterIDSource indicates how the router ID was resolved:
                  "explicit", "template", "node-ipv4", or "hash-from-node-name"
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
